import React from 'react';
import LoginForm from '../components/LoginForm';

const Home = () => {
  return (
    
    <div style={{ padding: '2rem' }}>
      <LoginForm />
    </div>
  );
};

export default Home;
